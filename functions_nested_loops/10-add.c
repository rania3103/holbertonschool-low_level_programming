#include "main.h"
/**
* add - Entry point
*
* Description: adds two integers and returns the result.
*@x :integer
@y :integer
* Return: Always 0 (Success)
*/
int add(int x, int y)
{
	return (x + y);
}
