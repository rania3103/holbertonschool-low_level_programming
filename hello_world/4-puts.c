#include <stdio.h>

/**
 * main - Entry point
 *
 * Description:a C program that prints a message
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
